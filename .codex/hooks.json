{
  "description": "Codex command guard for Lane 2 raw-post enforcement (harmonic-forge#371). harmonic-forge itself has no per-repo gate_codex_tool.py wrapper (unlike HRSE2) -- the canonical hook module is wired directly, the same way HRSE2's hooks.json already does for model_tier_gate.py.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${HOME}/harmonic-forge/tools/hooks/block_lane2_status_claims.py\"",
            "timeout": 5,
            "statusMessage": "Checking Lane 2 posting integrity"
          }
        ]
      }
    ]
  }
}
